node_modules
dist
.env
.env.local
.env.*.local
.env.development
.env.production
.DS_Store
Thumbs.db
*.log
.claude/

# Extracted DynamoDBLocal.jar/libs + its persisted local dev data — neither
# belongs in git; setup-dynamodb-jar.js re-extracts the jar on demand.
dynamodb-local/
